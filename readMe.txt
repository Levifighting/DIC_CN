一、src目录包分类（com.stone.*）：

1、adapters——存放适配器的类

2、dic——主要目录，存放activity（命名规则：*Activity）

3、models——数据模型

4、services——访问后台服务的api接口

5、utils——各种工具类，如json解析

6、iplugins——自定义的控件

7、common——全局的公共类


二、libs

1、gson——做json解析的jar包

2、volley——向后台服务发消息的jar包


三、